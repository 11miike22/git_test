# StrideGo Landing Page

Simple static landing page for StrideGo.

## Deploy

Upload `index.html`, `styles.css`, `README.md`, and `.gitignore` to the repo root.
Vercel will serve `index.html` at `/` as a plain static site.
